# Broadsheet dependencies
# Install with: pip install -r requirements.txt

# Agent framework
crewai>=0.80.0
crewai[google-genai]>=0.80.0

# LLM integrations (OpenAI-compatible API client)
openai>=1.30.0
litellm>=1.0.0

# News fetching
feedparser>=6.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0    # GitHub Trending scraper

# Tavily search
tavily-python>=0.3.0

# Notion delivery
notion-client>=2.2.0,<3.0.0

# Topic clustering (C2 — heuristic scoring)
scikit-learn>=1.3.0

# Config and env
python-dotenv>=1.0.0
pyyaml>=6.0.1

# Python stdlib used: smtplib, email, json, logging, concurrent.futures
# (no extra install needed)
